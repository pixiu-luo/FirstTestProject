package main.testJava;

import java.lang.reflect.Method;

public class testTofanshe {
    public static void main(String[] args) {
        /*BiErrorControlLog biErrorControlLog = biErrorContrologService.selectById(id);
        BiErrorControlLogCompositeServiceImpl be = new BiErrorControlLogCompositeServiceImpl();
        Class<? extends BiErrorControlLogCompositeServiceImpl> c = be.getClass();
        //Class<?> cls = Class.forName("com.*****.BiErrorControlLogCompositeServiceImpl");
        //Object obj = ApplicationContextHolder.getBeanByType(cls);
        //PafaApplicationContext context = (PafaApplicationContext) AppContext.getInstance().getApplicationContext();
        //BeanFactory factory = (BeanFactory) context;
        //Object obj = factory.getBean("orderService");  
        //Object obj = factory.getBean(interfaceName);
        Object obj = c.newInstance();
        Method m = c.getDeclaredMethod("do" + biErrorControlLog.getMethodName(), String.class);
        m.invoke(obj, id);*/
    }
}
